# secrets — never committed, ever
.env
.env.*
!.env.example
HANDOFF.md
*HANDOFF*

# private business planning + working conventions — never committed (public repo)
planning/
CLAUDE.md
.claude/

# private user data (also lives outside the repo tree on the server)
data/private/
*.db
*.sqlite
*.sqlite3
*.log
*.log.*
logs/

# deps & build output
src/generated/
node_modules/
dist/
.astro/
.og-cache.json

# editor / OS junk
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# heavy AI direction boards live outside the repo (brand/prompts.md documents them)
brand/direction-board*.png
